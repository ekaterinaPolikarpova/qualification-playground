package PreviousChanges.patternTest;

import java.util.ArrayList;
import java.util.List;

public class Ex {
    public static List<String> names = new ArrayList<>();
    public static List<Integer> nums = new ArrayList<>();
    public static int age;
    public List<Ex> exes = new ArrayList<>();
    public List<List<Integer>> ll = new ArrayList<>();

    public int getAge() {
        return age;
    }

    public void check() {
//        Stream<String> stringList = names.stream();
//        List<Integer> numStream = nums.stream().filter(n -> n % 2 == 0).toList();
//        List<String> ascNames = names.stream().sorted().toList();
//        List<Integer> length = names.stream().map(String::length).toList();
//        Map<Integer, List <Ex>> groupedByAge = exes.stream().collect(Collectors.groupingBy(Ex::getAge));
//        boolean y = names.stream().distinct().count() != names.size();
//        List <String> u = Stream.concat(names.stream(), exes.stream()).toList();
//        Integer s = nums.stream().mapToInt(Integer::intValue).sum();
//        Integer s = nums.stream().distinct().toList();
//        List<Integer> ints = nums.stream().skip(6).limit(5).toList();

    }
}
